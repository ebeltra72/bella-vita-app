[build]
  command = "npm run build"
  publish = "dist"
  functions = "netlify/functions"

[build.environment]
  NODE_VERSION = "18"

[secrets_scanning]
  enabled = true
  omit_keys = ["CLOUDINARY_API_SECRET"]
